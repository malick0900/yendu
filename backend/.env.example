# --- MongoDB ---
MONGO_URL=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=teranga_stay

# --- App ---
JWT_SECRET=change-me-to-a-long-random-string
APP_NAME=teranga-stay
# Comma-separated list, or "*" (echoes origin) for dev only
CORS_ORIGINS=https://your-frontend.vercel.app

# --- Google OAuth (Google Identity Services) ---
# OAuth 2.0 Client ID from https://console.cloud.google.com/apis/credentials
# Type: "Web application". Same value goes in frontend REACT_APP_GOOGLE_CLIENT_ID.
GOOGLE_CLIENT_ID=

# --- Cloudflare R2 (image uploads) ---
# Account ID from Cloudflare dashboard. Create R2 bucket + API token (Object Read & Write).
R2_ACCOUNT_ID=
R2_BUCKET=teranga-stay
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# Optional: override endpoint (defaults to https://<account>.r2.cloudflarestorage.com)
# R2_ENDPOINT=

# --- Email (Resend SMTP) ---
# Sign up at https://resend.com, create an API key in Settings → API Keys.
# Until you verify a custom domain, use MAIL_FROM="Yendu <onboarding@resend.dev>".
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=
MAIL_FROM=Yendu <onboarding@resend.dev>

# --- Background jobs ---
# Frontend URL embedded in the review-request emails (no trailing slash)
FRONTEND_URL=https://yendu.vercel.app
# Days after the stay end_date before sending the review-request email
REVIEW_EMAIL_DELAY_DAYS=2
